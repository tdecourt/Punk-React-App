image: node:latest

pages:
  tags:
    - docker
  stage: deploy
  cache:
    paths:
      - node_modules/
  script:
    - npm install
    - npm run build
    - serve -s build
  artifacts:
    paths:
      - public
  only:
    - main